# Protocol：A11_23b 显式绑定校准

用户已授权阻塞修复。将每个关系映射编码为带表角色的原子 pair token，保持潜在函数 $Y=\pi_2(\pi_1(A))$ 不变。使用两层 width-64 Transformer、全局互斥置换 split、五 seed、最多 3000 NTP steps。通过要求 test 答案 `5/5 >=0.70`、格式 `>=0.95`、任一单表缺失控制 `<0.25`。成功只说明最小符号组合表面有效，不能声称长上下文或 MTP 有益。
