# MTP Long-Horizon Theory Sync

Date: 2026-07-08

Purpose: sync the self-contained theory story for the controlled MTP vs NTP
long-horizon line.

Read first:

```text
01_long_horizon_mtp_self_contained_story_v0.md
```

Core claim: in a fixed controlled long-horizon framework, next-$K$ prediction
has a representation advantage over next-token prediction when $K$ covers the
first future token that contains the early semantic variable.

Boundary: this package is a theory/story note. It does not include raw
experiment logs, checkpoints, datasets, or code workspaces.
